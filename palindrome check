#include <iostream>
#include<cmath>
using namespace std;
bool reverse(int n){
    int temp=n,sum=0;
    while(n){
        sum=sum*10+n%10;
        n/=10;
    }
    return sum==temp;
}
int main() {
    int n;
    cin>>n;
cout<<reverse(n);
    return 0;
}
output
111
1
